import React from 'react';
import './searchbutton.css'

const SearchButton = (props) => {
 return (
   <a href="#download" className="btn btn-outline btn-xl btn-block" id="startAnalysis" >Start Analysis</a>
   );
};

export default SearchButton
